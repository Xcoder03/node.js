// this folder acts as your helper functions(login, validation) just like the utils folder 
// but with slight difference

export const sampleMiddleware = async(req,res) => {
    try {
        return res.json({ })
    } catch (error) {
        
    }
   
}

